import React, { useEffect, useMemo, useState } from "react";

const iconMap = {
  book: "M4 19.5A2.5 2.5 0 0 1 6.5 17H20 M4 4.5A2.5 2.5 0 0 1 6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5z",
  calendar: "M7 2v4 M17 2v4 M3 10h18 M5 6h14a2 2 0 0 1 2 2v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2z",
  chevronRight: "M9 18l6-6-6-6",
  copy: "M8 8h10v12H8z M6 16H4V4h12v2",
  github: "M9 19c-5 1.5-5-2.5-7-3 M15 22v-3.5c0-1 .2-1.7-.5-2 3.2-.4 6.5-1.6 6.5-7A5.4 5.4 0 0 0 19.5 5 5 5 0 0 0 19.4 1s-1.3-.4-4.4 1.6a15.4 15.4 0 0 0-8 0C3.9.6 2.6 1 2.6 1a5 5 0 0 0-.1 4A5.4 5.4 0 0 0 1 9.5c0 5.4 3.3 6.6 6.5 7-.4.2-.7.7-.8 1.3-.7.3-2.5.8-3.7-1",
  home: "M3 11l9-8 9 8 M5 10v11h14V10 M9 21v-7h6v7",
  layers: "M12 2l10 5-10 5L2 7l10-5z M2 12l10 5 10-5 M2 17l10 5 10-5",
  moon: "M21 12.8A9 9 0 1 1 11.2 3 7 7 0 0 0 21 12.8z",
  pen: "M12 20h9 M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z",
  search: "M21 21l-4.3-4.3 M10.5 18a7.5 7.5 0 1 1 0-15 7.5 7.5 0 0 1 0 15z",
  sparkles: "M12 3l1.8 5.2L19 10l-5.2 1.8L12 17l-1.8-5.2L5 10l5.2-1.8L12 3z M5 3v4 M3 5h4 M19 17v4 M17 19h4",
  sun: "M12 4V2 M12 22v-2 M4.9 4.9 3.5 3.5 M20.5 20.5l-1.4-1.4 M4 12H2 M22 12h-2 M4.9 19.1l-1.4 1.4 M20.5 3.5l-1.4 1.4 M12 17a5 5 0 1 0 0-10 5 5 0 0 0 0 10z",
  tag: "M20.6 13.4 13.4 20.6a2 2 0 0 1-2.8 0L3 13V3h10l7.6 7.6a2 2 0 0 1 0 2.8z M7.5 7.5h.01",
  user: "M20 21a8 8 0 0 0-16 0 M12 13a5 5 0 1 0 0-10 5 5 0 0 0 0 10z",
};

const themes = [
  { id: "light", name: "清雅白", icon: "sun", vars: { "--bg": "#f8fafc", "--panel": "#ffffff", "--soft": "#f1f5f9", "--text": "#0f172a", "--muted": "#64748b", "--line": "#e2e8f0", "--accent": "#2563eb", "--accent-soft": "#dbeafe", "--shadow": "0 22px 55px rgba(15, 23, 42, 0.08)" } },
  { id: "ink", name: "墨黑夜读", icon: "moon", vars: { "--bg": "#09090b", "--panel": "#18181b", "--soft": "#27272a", "--text": "#fafafa", "--muted": "#a1a1aa", "--line": "#3f3f46", "--accent": "#a78bfa", "--accent-soft": "#2e1065", "--shadow": "0 22px 55px rgba(0, 0, 0, 0.35)" } },
  { id: "paper", name: "暖纸笔记", icon: "book", vars: { "--bg": "#fbf3e4", "--panel": "#fffaf0", "--soft": "#f7e8c8", "--text": "#342313", "--muted": "#8a6a43", "--line": "#ead7b7", "--accent": "#b45309", "--accent-soft": "#fed7aa", "--shadow": "0 22px 55px rgba(120, 53, 15, 0.12)" } },
  { id: "terminal", name: "终端青", icon: "layers", vars: { "--bg": "#061014", "--panel": "#0b1f26", "--soft": "#10323b", "--text": "#e6fffb", "--muted": "#8dd9d2", "--line": "#164e58", "--accent": "#22d3ee", "--accent-soft": "#083344", "--shadow": "0 22px 55px rgba(0, 0, 0, 0.38)" } },
];

const seriesList = [
  { id: "rtos", title: "RTOS 多任务设计系列", desc: "围绕任务、事件、队列、状态机、共享资源和调度边界，建立可复用的 RTOS 设计方法。", level: "架构能力" },
  { id: "tbox", title: "TBOX 低功耗实战系列", desc: "分层拆解系统休眠、外设唤醒、总线使能、协议执行和云端上报链路。", level: "项目复盘" },
  { id: "driver", title: "驱动与协议调试系列", desc: "聚焦 UART、DMA、RS485、协议解析、日志插桩和最小验证闭环。", level: "底层能力" },
  { id: "growth", title: "嵌入式架构师成长系列", desc: "沉淀源码阅读、工程复盘、架构取舍和长期学习路线。", level: "成长路径" },
];

const posts = [
  { slug: "rtos-task-design", title: "RTOS 多任务设计：事件、队列、状态机如何分层", date: "2026-05-27", updated: "2026-05-27", category: "嵌入式架构", series: "rtos", quality: "架构", tags: ["RTOS", "ThreadX", "状态机"], minutes: 12, summary: "从输入源、实时性、阻塞点、共享资源四个维度，拆解一个稳定 RTOS 业务框架的设计方法。", content: [{ heading: "结论", body: "RTOS 多任务设计的核心不是任务越多越好，而是先识别输入源、实时性、阻塞点和共享资源，再决定任务、事件、队列、状态机的边界。" }, { heading: "设计框架", body: "事件负责通知，队列负责传数据，状态机负责阶段切换，任务负责承载执行上下文。不要用队列传递一切，也不要让多个任务随意改同一个全局状态。" }, { heading: "最小落地", body: "新项目先画输入源清单，再标出哪些逻辑必须实时响应，哪些逻辑可以排队处理，哪些资源存在并发访问风险。最后再决定任务数量和优先级。" }, { heading: "验证方法", body: "用日志记录事件进入、队列入队、状态切换、任务处理耗时和异常分支。通过这些点验证系统是否出现队列堆积、事件丢失、状态跳变或优先级设计错误。" }] },
  { slug: "tbox-low-power-chain", title: "TBOX 低功耗链路复盘：唤醒信号不等于系统唤醒", date: "2026-05-22", updated: "2026-05-27", category: "TBOX 实战", series: "tbox", quality: "复盘", tags: ["TBOX", "低功耗", "WAKEUP_OUT"], minutes: 9, summary: "把系统休眠、外设唤醒、总线使能、协议执行、云端上报拆成独立链路，避免误判。", content: [{ heading: "结论", body: "WAKEUP_OUT 只能说明外设侧被拉醒或保持，不能直接证明主系统已经退出低功耗。系统状态、模块状态、总线状态和云端上报状态必须分开验证。" }, { heading: "分层链路", body: "低功耗问题建议拆成：系统休眠/唤醒、外设/模块唤醒、总线使能、命令接收、协议解析、本地执行、ACK 反馈、事件上报、属性回云、云端确认。" }, { heading: "常见误判", body: "发出请求不等于执行成功，收到命令不等于状态生效，ACK 成功不等于事件上报成功，上报成功也不等于云端确认成功。" }, { heading: "验证方法", body: "优先插桩系统低功耗状态变量、WAKEUP_OUT 控制点、RS485 报文收发点、云端事件上报返回值和离线缓存补发路径。" }] },
  { slug: "uart-dma-idle-parser", title: "UART DMA + IDLE 接收：为什么协议层还需要流式解析", date: "2026-05-18", updated: "2026-05-18", category: "驱动开发", series: "driver", quality: "源码", tags: ["UART", "DMA", "协议解析"], minutes: 10, summary: "DMA/IDLE 只负责搬运字节，不负责保证一帧完整。协议层必须处理半包、粘包、错包。", content: [{ heading: "结论", body: "DMA 空闲中断只能说明一段时间内没有新字节到达，不能保证本次收到的数据就是完整协议帧。协议层仍然需要流式状态机。" }, { heading: "驱动边界", body: "驱动层负责收字节、搬运缓存、触发通知；协议层负责固定头识别、长度校验、CRC 校验、半包缓存、粘包拆分和异常恢复。" }, { heading: "推荐结构", code: "IDLE中断 -> DMA长度计算 -> RX队列 -> 协议状态机 -> 完整帧事件 -> 业务任务" }, { heading: "验证方法", body: "用测试数据覆盖正常帧、半包、粘包、错头、错长度、错 CRC、超长帧和连续高频帧。" }] },
  { slug: "source-reading-method", title: "源码阅读方法：先抓结构体，再看谁赋值、谁读取", date: "2026-05-12", updated: "2026-05-12", category: "学习方法", series: "growth", quality: "方法", tags: ["源码阅读", "工程能力", "方法论"], minutes: 7, summary: "读源码不要只看接口名，先找核心结构体和调用链，再结合日志验证真实语义。", content: [{ heading: "结论", body: "变量名、函数名、状态名只能作为线索，不能直接当真实语义。真正可靠的是谁初始化、谁赋值、谁读取、在哪条调用链生效。" }, { heading: "阅读顺序", body: "先找核心结构体，再看初始化函数，然后看写入点、读取点、状态切换点、错误处理点和日志输出点。" }, { heading: "工程意义", body: "这种方法能避免只看接口名就下结论，尤其适合 RTOS、驱动框架、协议栈、低功耗状态机这类跨模块代码。" }, { heading: "验证方法", body: "用 grep/全局搜索追踪字段读写，再结合断点、日志、运行状态去确认推断是否成立。" }] },
];

const readingPaths = [
  { title: "嵌入式初学者", desc: "先建立驱动、协议、RTOS 的基础工程视角。", slugs: ["uart-dma-idle-parser", "rtos-task-design", "source-reading-method"] },
  { title: "正在做 TBOX", desc: "优先理解低功耗、总线、事件上报和工程复盘链路。", slugs: ["tbox-low-power-chain", "rtos-task-design", "uart-dma-idle-parser"] },
  { title: "想提升架构能力", desc: "从任务边界、状态机、源码阅读和复盘模板开始。", slugs: ["rtos-task-design", "source-reading-method", "tbox-low-power-chain"] },
];

const writingTemplate = `---
title: "这里填写文章标题"
date: "2026-05-27"
category: "嵌入式实战"
series: "tbox"
tags: ["RTOS", "调试", "状态机"]
summary: "一句话说明这篇文章解决什么问题。"
---

# 结论

先给出核心判断。

# 背景

说明项目场景、问题现象、版本、复现条件。

# 已知事实

只写已经被源码、日志、报文或实验确认的信息。

# 原因分析

区分事实、推断、假设。

# 最小修改方案

说明改哪里、为什么、影响范围。

# 验证方法

列出日志、测试用例、通过标准。

# 风险与复盘

沉淀经验，避免下次重复踩坑。
`;

const styles = `*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI","Microsoft YaHei",sans-serif}button,input{font:inherit}button{cursor:pointer}a{color:inherit;text-decoration:none}.blog-app{min-height:100vh;background:var(--bg);color:var(--text);transition:background .25s ease,color .25s ease}.read-progress{position:fixed;left:0;top:0;height:3px;width:var(--progress);background:var(--accent);z-index:50;transition:width .1s linear}.container{width:min(1180px,calc(100% - 32px));margin:0 auto;padding:24px 0 36px}.nav{display:flex;justify-content:space-between;align-items:center;gap:16px;padding:16px;border:1px solid var(--line);border-radius:28px;background:var(--panel);box-shadow:var(--shadow);position:sticky;top:14px;z-index:10}.brand{display:flex;align-items:center;gap:12px}.logo{width:46px;height:46px;display:grid;place-items:center;border-radius:18px;color:var(--accent);background:var(--accent-soft);flex:0 0 auto}.brand-title{margin:0;font-size:18px;font-weight:900;letter-spacing:-.02em}.brand-subtitle{margin:3px 0 0;color:var(--muted);font-size:13px}.nav-links,.theme-list{display:flex;flex-wrap:wrap;align-items:center;gap:8px}.nav-links{justify-content:center}.nav-link,.theme-btn,.pill-btn,.primary-btn,.secondary-btn,.tag-btn,.text-btn{border-radius:999px;transition:transform .18s ease,border-color .18s ease,opacity .18s ease,background .18s ease}.nav-link,.theme-btn{display:inline-flex;align-items:center;gap:7px;padding:9px 12px;border:1px solid var(--line);background:var(--soft);color:var(--muted);font-size:13px}.nav-link.active,.theme-btn.active{border-color:var(--accent);background:var(--accent-soft);color:var(--accent);font-weight:800}.nav-link:hover,.theme-btn:hover,.pill-btn:hover,.tag-btn:hover,.secondary-btn:hover,.text-btn:hover{transform:translateY(-1px);border-color:var(--accent)}.hero-grid{display:grid;grid-template-columns:1.12fr .88fr;gap:22px;padding:30px 0 22px}.card{border:1px solid var(--line);border-radius:32px;background:var(--panel);box-shadow:var(--shadow)}.hero-card{padding:38px;animation:rise .48s ease both}.badge{display:inline-flex;align-items:center;gap:8px;padding:9px 14px;border-radius:999px;background:var(--accent-soft);color:var(--accent);font-size:14px;font-weight:800}h1{max-width:820px;margin:22px 0 0;font-size:clamp(36px,6vw,66px);line-height:1.03;letter-spacing:-.06em}.hero-desc{max-width:760px;margin:22px 0 0;color:var(--muted);font-size:17px;line-height:1.9}.hero-actions{display:flex;flex-wrap:wrap;gap:12px;margin-top:28px}.primary-btn,.secondary-btn,.text-btn{border:0;display:inline-flex;align-items:center;gap:8px}.primary-btn{padding:13px 19px;background:var(--accent);color:white;font-weight:900;box-shadow:0 14px 30px rgba(37,99,235,.16)}.secondary-btn{padding:12px 18px;border:1px solid var(--line);background:var(--soft);color:var(--text);font-weight:800}.text-btn{padding:9px 12px;border:1px solid var(--line);background:transparent;color:var(--muted);font-size:13px}.workbench{padding:26px;animation:rise .48s .08s ease both}.card-title-row{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:18px}.card-title-row h2,.side-card h3{margin:0}.small-badge{padding:6px 10px;border-radius:999px;background:var(--soft);color:var(--muted);font-size:12px}.workflow,.series-grid,.path-grid{display:grid;gap:12px}.workflow-item,.mini-card{padding:15px;border-radius:20px;background:var(--soft)}.workflow-item strong,.mini-card strong{display:block;margin-bottom:6px}.workflow-item span,.mini-card span{color:var(--muted);font-size:14px;line-height:1.65}.section-title{display:flex;align-items:end;justify-content:space-between;gap:16px;margin:20px 0 14px}.section-title h2{margin:0;font-size:26px;letter-spacing:-.03em}.section-title p{margin:6px 0 0;color:var(--muted);line-height:1.7}.content-grid{display:grid;grid-template-columns:330px 1fr;gap:22px}.sidebar{display:grid;align-content:start;gap:16px}.side-card{padding:20px}.search-box{display:flex;align-items:center;gap:9px;padding:12px 14px;border-radius:18px;background:var(--soft);color:var(--muted)}.search-box input{width:100%;border:0;outline:0;background:transparent;color:var(--text)}.filter-list,.tag-list,.post-tags{display:flex;flex-wrap:wrap;gap:8px;margin-top:14px}.pill-btn{padding:9px 12px;background:var(--soft);color:var(--muted);font-size:13px;border:1px solid transparent}.pill-btn.active{background:var(--accent);color:white;font-weight:800}.tag-btn{padding:8px 11px;border:1px solid var(--line);background:transparent;color:var(--muted);font-size:13px}.side-card p,.deploy-list{color:var(--muted);font-size:14px;line-height:1.8}.deploy-list{margin:12px 0 0;padding-left:20px}.main-grid{display:grid;grid-template-columns:minmax(0,1fr) minmax(320px,.92fr);gap:22px}.post-list{display:grid;align-content:start;gap:14px}.post-card{text-align:left;width:100%;padding:20px;border:1px solid var(--line);border-radius:26px;background:var(--panel);color:var(--text);box-shadow:var(--shadow)}.post-card.active{border-color:var(--accent);background:var(--accent-soft)}.post-card:hover{transform:translateY(-2px);border-color:var(--accent)}.post-meta{display:flex;flex-wrap:wrap;gap:12px;align-items:center;color:var(--muted);font-size:12px}.post-card h2{margin:12px 0 0;font-size:20px;line-height:1.35}.post-card p{margin:12px 0 0;color:var(--muted);line-height:1.75;font-size:14px}.post-tags span{padding:6px 10px;border-radius:999px;background:var(--soft);color:var(--muted);font-size:12px}.preview-card,.toc-card{padding:26px;position:sticky;top:112px;align-self:start}.preview-head{display:flex;justify-content:space-between;align-items:center;gap:16px;margin-bottom:18px}.category{padding:8px 12px;border-radius:999px;background:var(--accent-soft);color:var(--accent);font-weight:800;font-size:13px}.preview-card h2{margin:0;font-size:clamp(26px,3.2vw,36px);line-height:1.18;letter-spacing:-.04em}.preview-meta{color:var(--muted);font-size:14px;margin:14px 0 0}.preview-content{margin-top:22px;padding:20px;border-radius:24px;background:var(--soft);line-height:1.95}.empty{padding:24px;color:var(--muted);line-height:1.8}.article-layout{display:grid;grid-template-columns:minmax(0,1fr) 290px;gap:22px;padding-top:28px}.article{padding:34px}.article h1{margin-top:10px;font-size:clamp(34px,5vw,56px)}.article-summary{margin:18px 0 0;color:var(--muted);font-size:17px;line-height:1.9}.article-section{padding-top:26px;margin-top:24px;border-top:1px solid var(--line)}.article-section h2{margin:0 0 12px;font-size:24px}.article-section p{color:var(--text);line-height:2;margin:0}.code-box{margin-top:12px;padding:16px;border-radius:18px;background:var(--soft);color:var(--text);overflow-x:auto;font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;font-size:14px}.toc-card h3{margin:0 0 12px}.toc-card a{display:block;padding:9px 0;color:var(--muted);border-bottom:1px solid var(--line);font-size:14px}.toc-card a:hover{color:var(--accent)}.related-grid,.about-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px;margin-top:16px}.about-page,.series-page{padding-top:28px}.about-hero,.series-hero{padding:34px}.about-hero h1,.series-hero h1{margin-top:0}.about-card{padding:22px}.footer{display:flex;justify-content:space-between;align-items:center;gap:14px;margin-top:28px;padding:18px 20px;color:var(--muted);font-size:14px}.footer-right{display:flex;align-items:center;gap:8px}.toast{position:fixed;right:20px;bottom:20px;padding:13px 16px;border:1px solid var(--line);border-radius:16px;background:var(--panel);color:var(--text);box-shadow:var(--shadow);z-index:30}@keyframes rise{from{opacity:0;transform:translateY(14px)}to{opacity:1;transform:translateY(0)}}@media(max-width:980px){.nav,.footer{align-items:flex-start;flex-direction:column}.nav-links,.theme-list{justify-content:flex-start}.hero-grid,.content-grid,.main-grid,.article-layout,.related-grid,.about-grid{grid-template-columns:1fr}.preview-card,.toc-card{position:static}}@media(max-width:560px){.container{width:min(100% - 20px,1180px);padding-top:12px}.hero-card,.article,.about-hero,.series-hero{padding:24px}.workbench,.preview-card,.side-card,.toc-card{padding:18px}h1{letter-spacing:-.04em}}`;

function Icon({ name, size = 18, className = "" }) { return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true" className={className}><path d={iconMap[name] || iconMap.book} /></svg>; }
function getCategories(postList) { return ["全部", ...Array.from(new Set(postList.map((post) => post.category)))]; }
function getAllTags(postList) { return Array.from(new Set(postList.flatMap((post) => post.tags))); }
function getSeries(id) { return seriesList.find((series) => series.id === id); }
function getPostBySlug(slug) { return posts.find((post) => post.slug === slug); }
function getPostsBySeries(seriesId) { return posts.filter((post) => post.series === seriesId); }
function getRelatedPosts(post) { return posts.filter((item) => item.slug !== post.slug && (item.series === post.series || item.category === post.category || item.tags.some((tag) => post.tags.includes(tag)))).slice(0, 3); }
function filterPosts(postList, query, activeCategory) { const keyword = query.trim().toLowerCase(); return postList.filter((post) => { const matchCategory = activeCategory === "全部" || post.category === activeCategory; const haystack = [post.title, post.summary, post.category, post.quality, ...post.tags].join(" ").toLowerCase(); return matchCategory && (!keyword || haystack.includes(keyword)); }); }
function parseRoute(hash) { const clean = hash.replace(/^#\/?/, ""); if (!clean) return { page: "home" }; const [page, slug] = clean.split("/"); if (page === "posts") return { page: "post", slug }; if (page === "series") return { page: "series", slug }; if (page === "about") return { page: "about" }; return { page: "home" }; }
function postPath(slug) { return `#/posts/${slug}`; }
function seriesPath(id) { return `#/series/${id}`; }
function sectionId(text) { return text.toLowerCase().replace(/[：:，,。./\\\s]+/g, "-").replace(/^-+|-+$/g, ""); }
function upsertMeta(selector, attrs) { let node = document.head.querySelector(selector); if (!node) { node = document.createElement("meta"); Object.entries(attrs.seed || {}).forEach(([key, value]) => node.setAttribute(key, value)); document.head.appendChild(node); } Object.entries(attrs.values).forEach(([key, value]) => node.setAttribute(key, value)); }
function updateSeo(route) { const post = route.page === "post" ? getPostBySlug(route.slug) : null; const series = route.page === "series" ? getSeries(route.slug) : null; const title = post ? `${post.title} | 青山技术札记` : series ? `${series.title} | 青山技术札记` : route.page === "about" ? "关于青山 | 青山技术札记" : "青山技术札记 | 嵌入式工程师的 RTOS、驱动、TBOX 实战笔记"; const desc = post?.summary || series?.desc || "专注 MCU / MPU / RTOS / 通信协议 / 车载 TBOX，把项目调试经验沉淀为可复用的工程方法。"; document.title = title; upsertMeta('meta[name="description"]', { seed: { name: "description" }, values: { content: desc } }); upsertMeta('meta[name="keywords"]', { seed: { name: "keywords" }, values: { content: "嵌入式,RTOS,ThreadX,TBOX,UART,DMA,RS485,低功耗,系统架构" } }); upsertMeta('meta[property="og:title"]', { seed: { property: "og:title" }, values: { content: title } }); upsertMeta('meta[property="og:description"]', { seed: { property: "og:description" }, values: { content: desc } }); }
function runSmokeTests() { console.assert(posts.length > 0, "Blog should contain posts."); console.assert(themes.length >= 2, "Blog should contain themes."); console.assert(posts.every((post) => post.slug && post.title && post.date && post.category && post.series && post.summary && Array.isArray(post.content)), "Every post should have required fields."); console.assert(seriesList.every((series) => posts.some((post) => post.series === series.id)), "Every series should contain at least one post."); console.assert(parseRoute("#/posts/rtos-task-design").slug === "rtos-task-design", "Hash route parser should parse post slug."); console.assert(getRelatedPosts(posts[0]).length > 0, "Related posts should be generated."); console.assert(filterPosts(posts, "ThreadX", "全部").length === 1, "Search should match post tags."); console.assert(filterPosts(posts, "", "驱动开发").length === 1, "Category filter should work."); console.assert(filterPosts(posts, "不存在的关键词", "全部").length === 0, "Search should support empty result."); }
if (typeof window !== "undefined") runSmokeTests();
function getInitialThemeId() { if (typeof window === "undefined") return "light"; const saved = window.localStorage.getItem("blog-theme-id"); return themes.some((theme) => theme.id === saved) ? saved : "light"; }
function PostCard({ post, active = false, compact = false }) { return <a href={postPath(post.slug)} className={`post-card ${active ? "active" : ""}`}><div className="post-meta"><span><Icon name="calendar" size={14} /> {post.date}</span><span>{post.minutes} min read</span><span>{post.quality}</span></div><h2>{post.title}</h2>{!compact && <p>{post.summary}</p>}<div className="post-tags">{post.tags.map((tag) => <span key={tag}>#{tag}</span>)}</div></a>; }
function HomePage({ query, setQuery, activeCategory, setActiveCategory, copyTemplate }) { const categories = useMemo(() => getCategories(posts), []); const allTags = useMemo(() => getAllTags(posts), []); const filteredPosts = useMemo(() => filterPosts(posts, query, activeCategory), [query, activeCategory]); const previewPost = filteredPosts[0] || posts[0]; return <><section className="hero-grid"><div className="card hero-card"><div className="badge"><Icon name="sparkles" size={16} />嵌入式工程师的项目实战复盘博客</div><h1>把调试经验，沉淀成可复用的工程知识库。</h1><p className="hero-desc">专注 MCU / MPU / RTOS / 通信协议 / 车载 TBOX，把真实项目里的问题定位、最小修改、验证闭环和架构取舍沉淀成长期可复用的方法。</p><div className="hero-actions"><button type="button" className="primary-btn" onClick={copyTemplate}><Icon name="copy" size={17} />复制写作模板</button><a className="secondary-btn" href="#posts">查看文章</a><a className="secondary-btn" href="#/about">关于我</a></div></div><aside className="card workbench"><div className="card-title-row"><h2>写作工作台</h2><span className="small-badge">V1.1 Knowledge Base</span></div><div className="workflow">{[["现象", "环境、版本、复现条件"], ["事实", "源码、日志、报文、实验结果"], ["假设", "最高优先级原因和验证方式"], ["闭环", "最小修改、回归测试、复盘沉淀"]].map(([title, desc]) => <div key={title} className="workflow-item"><strong>{title}</strong><span>{desc}</span></div>)}</div></aside></section><section><div className="section-title"><div><h2>专题系列</h2><p>用系列串联文章，让一次访问变成连续学习路径。</p></div><a className="text-btn" href="#/series/rtos">查看系列</a></div><div className="series-grid">{seriesList.map((series) => <a key={series.id} className="mini-card" href={seriesPath(series.id)}><strong>{series.title}</strong><span>{series.desc}</span><div className="post-tags"><span>{series.level}</span><span>{getPostsBySeries(series.id).length} 篇</span></div></a>)}</div></section><section><div className="section-title"><div><h2>推荐阅读路径</h2><p>不同读者进入博客时，不需要自己猜该先看什么。</p></div></div><div className="path-grid">{readingPaths.map((path) => <div key={path.title} className="card side-card"><h3>{path.title}</h3><p>{path.desc}</p><div className="workflow">{path.slugs.map((slug, index) => { const post = getPostBySlug(slug); return <a key={slug} className="mini-card" href={postPath(slug)}><strong>{index + 1}. {post.title}</strong><span>{post.summary}</span></a>; })}</div></div>)}</div></section><section className="content-grid" id="posts"><aside className="sidebar"><div className="card side-card"><label className="search-box"><Icon name="search" size={18} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="搜索文章、标签、专题..." /></label><div className="filter-list">{categories.map((category) => <button key={category} type="button" onClick={() => setActiveCategory(category)} className={`pill-btn ${activeCategory === category ? "active" : ""}`}>{category}</button>)}</div></div><div className="card side-card"><h3><Icon name="tag" size={18} /> 热门标签</h3><div className="tag-list">{allTags.map((tag) => <button key={tag} type="button" onClick={() => setQuery(tag)} className="tag-btn">#{tag}</button>)}</div></div><div className="card side-card"><h3>产品化能力</h3><ol className="deploy-list"><li>文章详情页可单独分享。</li><li>专题系列串联内容。</li><li>About 页面建立个人定位。</li><li>SEO Meta 自动更新。</li></ol></div></aside><div className="main-grid"><section className="post-list">{filteredPosts.length === 0 ? <div className="card empty">没有匹配到文章。可以清空搜索关键词，或者切换到“全部”分类。</div> : filteredPosts.map((post) => <PostCard key={post.slug} post={post} active={post.slug === previewPost.slug} />)}</section><article className="card preview-card"><div className="preview-head"><span className="category">{previewPost.category}</span><Icon name="chevronRight" size={20} /></div><h2>{previewPost.title}</h2><p className="preview-meta">{previewPost.date} · {previewPost.minutes} 分钟阅读 · /posts/{previewPost.slug}</p><div className="preview-content">{previewPost.summary}</div><div className="hero-actions"><a className="primary-btn" href={postPath(previewPost.slug)}>阅读详情</a><a className="secondary-btn" href={seriesPath(previewPost.series)}>查看专题</a></div></article></div></section></>; }
function ArticlePage({ post }) { if (!post) return <div className="card empty">文章不存在。<a className="text-btn" href="#/">返回首页</a></div>; const series = getSeries(post.series); const related = getRelatedPosts(post); const seriesPosts = getPostsBySeries(post.series); const index = seriesPosts.findIndex((item) => item.slug === post.slug); const prev = seriesPosts[index - 1]; const next = seriesPosts[index + 1]; return <section className="article-layout"><article className="card article"><div className="badge">{post.quality} · {series?.title}</div><h1>{post.title}</h1><p className="article-summary">{post.summary}</p><p className="preview-meta">发布：{post.date} · 更新：{post.updated} · {post.minutes} 分钟阅读</p><div className="post-tags">{post.tags.map((tag) => <span key={tag}>#{tag}</span>)}</div>{post.content.map((block) => <section key={block.heading} id={sectionId(block.heading)} className="article-section"><h2>{block.heading}</h2>{block.body && <p>{block.body}</p>}{block.code && <pre className="code-box"><code>{block.code}</code></pre>}</section>)}<section className="article-section"><h2>上一篇 / 下一篇</h2><div className="related-grid">{prev ? <PostCard post={prev} compact /> : <div className="mini-card"><strong>已经是本系列第一篇</strong><span>可以继续阅读下一篇或返回专题页。</span></div>}{next ? <PostCard post={next} compact /> : <div className="mini-card"><strong>已经是本系列最后一篇</strong><span>可以查看相关文章或回到专题页。</span></div>}</div></section><section className="article-section"><h2>相关文章</h2><div className="related-grid">{related.map((item) => <PostCard key={item.slug} post={item} compact />)}</div></section></article><aside className="card toc-card"><h3>文章目录</h3>{post.content.map((block) => <a key={block.heading} href={`#${sectionId(block.heading)}`}>{block.heading}</a>)}<div className="hero-actions"><a className="secondary-btn" href={seriesPath(post.series)}>专题页</a><a className="secondary-btn" href="#/">首页</a></div></aside></section>; }
function AboutPage() { return <section className="about-page"><div className="card about-hero"><div className="badge"><Icon name="user" size={16} />关于青山</div><h1>嵌入式工程师，持续沉淀系统级问题解决能力。</h1><p className="hero-desc">我关注 MCU / MPU、RTOS / ThreadX、TBOX、UART / RS485 / BLE / 4G、低功耗、OTA、日志系统和嵌入式系统架构设计。这个博客不是简单搬运知识点，而是记录真实项目中的问题定位、工程取舍和复盘沉淀。</p></div><div className="about-grid"><div className="card about-card"><h2>我关注的方向</h2><ol className="deploy-list"><li>RTOS 多任务与状态机设计</li><li>驱动、中断、DMA 与协议解析</li><li>TBOX 低功耗、唤醒与云端上报链路</li><li>源码阅读、架构取舍与工程复盘</li></ol></div><div className="card about-card"><h2>博客写作原则</h2><ol className="deploy-list"><li>先给结论，再给依据。</li><li>事实、推断、假设分开。</li><li>优先最小修改和可验证闭环。</li><li>保留源码、日志、协议、状态机的原始名称。</li></ol></div></div></section>; }
function SeriesPage({ seriesId }) { const series = getSeries(seriesId) || seriesList[0]; const seriesPosts = getPostsBySeries(series.id); return <section className="series-page"><div className="card series-hero"><div className="badge"><Icon name="layers" size={16} />专题系列</div><h1>{series.title}</h1><p className="hero-desc">{series.desc}</p><div className="post-tags"><span>{series.level}</span><span>{seriesPosts.length} 篇文章</span></div></div><div className="section-title"><div><h2>系列文章</h2><p>建议按顺序阅读，形成完整知识链路。</p></div></div><div className="post-list">{seriesPosts.map((post) => <PostCard key={post.slug} post={post} />)}</div></section>; }
function BlogApp() { const [themeId, setThemeId] = useState(getInitialThemeId); const [query, setQuery] = useState(""); const [activeCategory, setActiveCategory] = useState("全部"); const [toast, setToast] = useState(""); const [route, setRoute] = useState(() => parseRoute(window.location.hash)); const [progress, setProgress] = useState(0); const theme = themes.find((item) => item.id === themeId) || themes[0]; const activePost = route.page === "post" ? getPostBySlug(route.slug) : null; useEffect(() => { window.localStorage.setItem("blog-theme-id", themeId); }, [themeId]); useEffect(() => { const onHash = () => { setRoute(parseRoute(window.location.hash)); window.scrollTo({ top: 0, behavior: "smooth" }); }; window.addEventListener("hashchange", onHash); return () => window.removeEventListener("hashchange", onHash); }, []); useEffect(() => { updateSeo(route); }, [route]); useEffect(() => { const onScroll = () => { const max = document.documentElement.scrollHeight - window.innerHeight; setProgress(max <= 0 ? 0 : Math.min(100, Math.round((window.scrollY / max) * 100))); }; onScroll(); window.addEventListener("scroll", onScroll); return () => window.removeEventListener("scroll", onScroll); }, [route]); useEffect(() => { if (!toast) return undefined; const timer = window.setTimeout(() => setToast(""), 2200); return () => window.clearTimeout(timer); }, [toast]); async function copyTemplate() { try { await navigator.clipboard.writeText(writingTemplate); setToast("Markdown 写作模板已复制"); } catch { setToast("当前浏览器不支持自动复制，可在代码中查看 writingTemplate"); } } return <main className="blog-app" style={{ ...theme.vars, "--progress": `${progress}%` }}><style>{styles}</style><div className="read-progress" /><div className="container"><nav className="nav"><a className="brand" href="#/"><div className="logo"><Icon name="pen" size={22} /></div><div><p className="brand-title">青山技术札记</p><p className="brand-subtitle">Embedded · Architecture · Notes</p></div></a><div className="nav-links"><a className={`nav-link ${route.page === "home" ? "active" : ""}`} href="#/"><Icon name="home" size={15} />首页</a><a className={`nav-link ${route.page === "series" ? "active" : ""}`} href="#/series/rtos"><Icon name="layers" size={15} />专题</a><a className={`nav-link ${route.page === "about" ? "active" : ""}`} href="#/about"><Icon name="user" size={15} />关于</a></div><div className="theme-list" aria-label="主题切换">{themes.map((item) => <button key={item.id} type="button" onClick={() => setThemeId(item.id)} className={`theme-btn ${item.id === themeId ? "active" : ""}`} aria-pressed={item.id === themeId}><Icon name={item.icon} size={15} />{item.name}</button>)}</div></nav>{route.page === "post" ? <ArticlePage post={activePost} /> : route.page === "about" ? <AboutPage /> : route.page === "series" ? <SeriesPage seriesId={route.slug} /> : <HomePage query={query} setQuery={setQuery} activeCategory={activeCategory} setActiveCategory={setActiveCategory} copyTemplate={copyTemplate} />}<footer className="card footer"><span>© 2026 青山技术札记 · 用文章记录工程判断和成长路径</span><span className="footer-right"><Icon name="github" size={17} /> GitHub Pages / RSS / About</span></footer></div>{toast ? <div className="toast" role="status">{toast}</div> : null}</main>; }
export default BlogApp;
